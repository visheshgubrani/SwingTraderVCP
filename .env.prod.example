# Copy to .env.prod on the VPS (never commit the filled file).
# VPS: 80.225.207.109
#
# Host ports chosen to avoid existing containers on this VPS:
#   open-webui 8080, academy 3000/5050, cramlify 3001/8001/5470
#
# Public hostnames (Caddy + TLS):
#   https://app.edurel.xyz  → personal client :8090
#   https://api.edurel.xyz  → FastAPI :8002

# --- Compose / images ---
GHCR_IMAGE_PREFIX=ghcr.io/visheshgubrani/swingtradervcp
IMAGE_TAG=latest
API_HOST_PORT=8002
CLIENT_HOST_PORT=8090
SWYINGIFY_HOST_PORT=3002

# --- Postgres (on VPS) ---
POSTGRES_USER=algo
POSTGRES_PASSWORD=replace-with-strong-password
POSTGRES_DB=algo_trading

# --- Redis (Upstash TCP — rediss:// with password) ---
REDIS_URL=rediss://default:REPLACE_UPSTASH_TOKEN@REPLACE.upstash.io:6379

# --- FastAPI ---
APP_ENVIRONMENT=production
CORS_ORIGINS=["https://app.edurel.xyz"]
FRONTEND_PUBLIC_URL=https://app.edurel.xyz
FYERS_APP_ID=
FYERS_SECRET_KEY=
# Must match the Fyers app redirect URI exactly (personal client callback).
FYERS_REDIRECT_URI=https://app.edurel.xyz/callback
EXECUTION_MODE=paper
LIVE_ORDER_PLACEMENT_ENABLED=false
SAAS_INTERNAL_API_KEY=replace-with-shared-random-secret

# Optional P7 (leave off for Step 1)
P7_FUNDAMENTAL_PASS_ENABLED=false

# --- Swyingify (Step 2 — fill when enabling --profile saas) ---
# BETTER_AUTH_SECRET=
# BETTER_AUTH_URL=
# NEXT_PUBLIC_BETTER_AUTH_URL=
# API_URL=http://api:8000
# SITE_URL=
# SEO_INDEXING_ENABLED=false
# SWYINGIFY_ADMIN_EMAILS=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
